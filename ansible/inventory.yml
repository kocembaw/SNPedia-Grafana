---
# Target host created by the Vagrantfile in the project root.
# Paths are relative to the ansible/ directory, so run playbooks from there.
all:
  hosts:
    snpedia-monitor:
      ansible_host: 192.168.56.10
      ansible_user: vagrant
      ansible_ssh_private_key_file: ../.vagrant/machines/default/virtualbox/private_key
